// const WebSocketServer = new require("ws");

// const wss = new WebSocketServer.Server({ port: 8080 });

// const kucoinws = new WebSocket("ws://api.kucoin.com/api/v1/bullet-public");
// kucoinws.onmessage = e => {
//   console.log(e.data);
// };

// ocket.addEventListener('open', function (event) {
//     const message = {
//         // тут Ви можете відправити POST запит з додатковими даними, які відповідають структурі API
//         // зазвичай, на сервері передбачені спеціальні формати даних для обміну повідомленнями через WebSocket
//     };
//     socket.send(JSON.stringify(message));
// });

// socket.addEventListener('message', function (event) {
//     console.log('Отримано повідомлення від сервера:', event.data);
// });

// socket.addEventListener('close', function (event) {
//     console.log('З'єднання закрито з причини:', event.code, event.reason);
// });

// socket.addEventListener('error', function (event) {
//     console.log('Сталася помилка:', event);
// });
